'use client'

// This component is no longer needed since page.tsx imports sections directly.
// Kept for backward compatibility. Can be removed.
export default function SceneRenderer() {
  return null
}